import math

class Solution(object):
    def isPerfectSquare(self, num):
        """
        :type num: int
        :rtype: bool
        """
        sqrt_num = math.sqrt(num)
        return int(sqrt_num) *int(sqrt_num) == num
